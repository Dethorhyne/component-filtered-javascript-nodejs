<% /*

Common javascript template which will be included in every page.
Can be used for layout generic actions/events which happen on every page

*/ %>

function insertResult(element, value)
{
    var element = document.getElementById(element)
    element.value = value;
}